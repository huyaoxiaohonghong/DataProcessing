# 生产环境配置
NODE_ENV=production

# 应用标题
VITE_APP_TITLE=数据处理系统

# API 基础路径
# 留空 -> 前端使用相对路径 /api/...，由 nginx.conf 的 location /api/ 反代到 backend:8000
# 这样无论用 IP 还是域名、http 还是 https 访问都能自动同源，省去跨域/证书问题
VITE_API_BASE_URL=

# API 前缀
VITE_API_PREFIX=/api

# 是否开启调试模式
VITE_DEBUG=false
